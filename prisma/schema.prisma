generator client {
	provider = "prisma-client-js"
}

datasource db {
	provider = "sqlite"
	url      = env("DATABASE_URL")
}

// Placeholder model to keep Prisma client usable until domain models are defined.
model Placeholder {
	id        String   @id @default(cuid())
	createdAt DateTime @default(now())
}
